# DREAM CAFE MODULE — Dream Place 내 통합 설계서

> **이 문서의 목적:** 드림 카페의 디지털 기능을 별도 앱이 아닌 **드림 플레이스(Dream Place) 앱 안에 모듈로 통합 구현**하기 위한 개발 지침서입니다.
> 
> **핵심 원칙:** 카페 전용 앱을 따로 만들지 않는다. 드림 플레이스 앱에 "Cafe" 탭/섹션을 추가하여 카페 관련 기능을 모두 제공한다.

---

## 1. 전체 구조 요약

드림 카페 모듈은 **3개의 시스템**으로 구성됩니다:

| 시스템 | 설명 | 사용자 |
|--------|------|--------|
| **Cafe Tab** (드림 플레이스 앱 내) | 체크인, 도어벨, 모바일 오더, 이벤트, 만남 기록 | 카페 방문 고객 |
| **Doorbell Display** (카페 대형 스크린용 웹앱) | 꿈 카드 슬라이드, 실시간 업데이트, 벨 알림 효과 | 카페 내 디스플레이 |
| **Cafe Admin** (관리자 웹 대시보드) | 이벤트 관리, 회원 관리, 도어벨 모니터링, 매출 확인 | 카페 직원/매니저 |

---

## 2. 데이터베이스 스키마 (새로 추가할 테이블)

### 2-1. cafes (카페 지점 정보)

```sql
CREATE TABLE cafes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,           -- 예: "Dream Cafe Gangnam"
  address TEXT NOT NULL,
  city VARCHAR(50) NOT NULL,
  country VARCHAR(50) NOT NULL,
  latitude DECIMAL(10, 8),
  longitude DECIMAL(11, 8),
  timezone VARCHAR(50) NOT NULL,        -- 예: "Asia/Seoul"
  operating_hours JSONB NOT NULL,       -- {"mon": {"open": "08:00", "close": "22:00"}, ...}
  status VARCHAR(20) DEFAULT 'active',  -- active, temporarily_closed, coming_soon
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 2-2. cafe_checkins (체크인/체크아웃)

```sql
CREATE TABLE cafe_checkins (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id),
  cafe_id UUID NOT NULL REFERENCES cafes(id),
  checked_in_at TIMESTAMPTZ DEFAULT NOW(),
  checked_out_at TIMESTAMPTZ,           -- NULL이면 아직 카페에 있음
  check_in_method VARCHAR(20),          -- 'qr', 'nfc', 'manual'
  CONSTRAINT fk_user FOREIGN KEY (user_id) REFERENCES users(id)
);
-- 인덱스: 현재 카페에 있는 사람 빠르게 조회
CREATE INDEX idx_active_checkins ON cafe_checkins(cafe_id, checked_out_at) WHERE checked_out_at IS NULL;
```

### 2-3. doorbell_dreams (도어벨에 등록된 꿈)

```sql
CREATE TABLE doorbell_dreams (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id),
  dream_statement VARCHAR(100) NOT NULL,  -- 꿈 한줄 선언문 (50자 이내 권장)
  categories VARCHAR(30)[] NOT NULL,      -- 예: ['tech', 'education', 'social_enterprise']
  needed_skills VARCHAR(30)[] DEFAULT '{}', -- 필요한 역량 태그 예: ['development', 'marketing']
  is_active BOOLEAN DEFAULT true,
  bell_count INTEGER DEFAULT 0,           -- 총 벨 받은 횟수
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

**카테고리 enum 값:**
- `tech` (기술/IT)
- `art_design` (예술/디자인)
- `education` (교육)
- `environment` (환경)
- `food` (음식/요리)
- `health_wellness` (건강/웰니스)
- `business` (비즈니스)
- `social_enterprise` (사회적기업)
- `other` (기타)

### 2-4. doorbell_rings (벨 누르기 기록)

```sql
CREATE TABLE doorbell_rings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ringer_id UUID NOT NULL REFERENCES users(id),       -- 벨을 누른 사람
  dream_id UUID NOT NULL REFERENCES doorbell_dreams(id), -- 대상 꿈
  dream_owner_id UUID NOT NULL REFERENCES users(id),  -- 꿈 주인
  cafe_id UUID REFERENCES cafes(id),                   -- 카페에서 눌렀으면 카페 ID
  status VARCHAR(20) DEFAULT 'pending',  -- pending, accepted, declined, expired
  message TEXT,                           -- 선택적 메시지
  rang_at TIMESTAMPTZ DEFAULT NOW(),
  responded_at TIMESTAMPTZ
);
```

### 2-5. cafe_events (카페 이벤트/프로그램)

```sql
CREATE TABLE cafe_events (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  cafe_id UUID NOT NULL REFERENCES cafes(id),
  title VARCHAR(200) NOT NULL,
  description TEXT,
  event_type VARCHAR(30) NOT NULL,  -- 'dream_talk', 'meetup', 'planner_workshop', 'dialogue', 'networking', 'dream5_matching', 'hackathon', 'launch_party', 'mentoring'
  start_time TIMESTAMPTZ NOT NULL,
  end_time TIMESTAMPTZ NOT NULL,
  max_participants INTEGER,
  current_participants INTEGER DEFAULT 0,
  price INTEGER DEFAULT 0,          -- 원 단위 (0이면 무료)
  currency VARCHAR(3) DEFAULT 'KRW',
  status VARCHAR(20) DEFAULT 'upcoming', -- upcoming, ongoing, completed, cancelled
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 2-6. cafe_event_registrations (이벤트 참가 신청)

```sql
CREATE TABLE cafe_event_registrations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  event_id UUID NOT NULL REFERENCES cafe_events(id),
  user_id UUID NOT NULL REFERENCES users(id),
  status VARCHAR(20) DEFAULT 'registered', -- registered, attended, cancelled, no_show
  registered_at TIMESTAMPTZ DEFAULT NOW(),
  attended_at TIMESTAMPTZ,
  UNIQUE(event_id, user_id)
);
```

### 2-7. cafe_meetings (만남 기록)

```sql
CREATE TABLE cafe_meetings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id),          -- 기록하는 사람
  met_user_id UUID NOT NULL REFERENCES users(id),      -- 만난 사람
  cafe_id UUID REFERENCES cafes(id),
  met_at TIMESTAMPTZ DEFAULT NOW(),
  memo TEXT,                             -- 만남 메모
  follow_up_action TEXT,                 -- 후속 조치
  source VARCHAR(30),                    -- 'doorbell', 'event', 'spontaneous'
  doorbell_ring_id UUID REFERENCES doorbell_rings(id), -- 도어벨로 만났으면
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 2-8. dialogue_bookings (다이얼로그 오브 드림스 예약)

```sql
CREATE TABLE dialogue_bookings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id),
  cafe_id UUID NOT NULL REFERENCES cafes(id),
  room_number INTEGER NOT NULL,          -- 다이얼로그 룸 번호 (1, 2, 3)
  booking_date DATE NOT NULL,
  start_time TIME NOT NULL,
  end_time TIME NOT NULL,                -- 보통 start_time + 90분 (대기10 + 체험60 + 리플렉션20)
  guide_name VARCHAR(100),
  status VARCHAR(20) DEFAULT 'confirmed', -- confirmed, completed, cancelled, no_show
  price INTEGER DEFAULT 50000,           -- 기본 5만원
  payment_status VARCHAR(20) DEFAULT 'pending',
  recording_url TEXT,                    -- 체험 녹음 파일 URL (완료 후)
  ai_report_id UUID,                     -- AI 분석 리포트 ID (드림 브레인 연동)
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 3. API 엔드포인트 설계

### 3-1. 카페 정보

```
GET    /api/cafe/locations                    -- 전체 카페 목록
GET    /api/cafe/:cafeId                      -- 특정 카페 상세 (영업시간, 위치 등)
GET    /api/cafe/:cafeId/status               -- 현재 상태 (영업중/마감, 체크인 드리머 수)
```

### 3-2. 체크인/체크아웃

```
POST   /api/cafe/:cafeId/checkin              -- 체크인 (body: { method: 'qr' | 'nfc' | 'manual' })
POST   /api/cafe/:cafeId/checkout             -- 체크아웃
GET    /api/cafe/:cafeId/current-dreamers     -- 현재 카페에 체크인한 드리머 목록 (프로필 카드)
```

### 3-3. 도어벨 (Doorbell)

```
POST   /api/doorbell/dreams                   -- 내 꿈 등록/수정
GET    /api/doorbell/dreams                   -- 전체 등록된 꿈 목록 (필터: 카테고리, 카페)
GET    /api/doorbell/dreams/:cafeId           -- 특정 카페의 도어벨 꿈 (체크인 유저 우선 정렬)
GET    /api/doorbell/dreams/mine              -- 내가 등록한 꿈
DELETE /api/doorbell/dreams/:dreamId          -- 내 꿈 삭제
POST   /api/doorbell/ring                     -- 벨 누르기 (body: { dreamId, message? })
GET    /api/doorbell/rings/received           -- 내가 받은 벨 목록
GET    /api/doorbell/rings/sent               -- 내가 보낸 벨 목록
PATCH  /api/doorbell/rings/:ringId            -- 벨 응답 (body: { status: 'accepted' | 'declined' })
```

### 3-4. 이벤트

```
GET    /api/cafe/:cafeId/events               -- 카페 이벤트 목록 (필터: upcoming, type)
GET    /api/cafe/events/:eventId              -- 이벤트 상세
POST   /api/cafe/events/:eventId/register     -- 이벤트 참가 신청
DELETE /api/cafe/events/:eventId/register     -- 참가 취소
GET    /api/cafe/events/my-registrations      -- 내가 신청한 이벤트 목록
```

### 3-5. 만남 기록

```
POST   /api/cafe/meetings                     -- 만남 기록 저장
GET    /api/cafe/meetings                     -- 내 만남 기록 목록
PATCH  /api/cafe/meetings/:meetingId          -- 만남 메모/후속조치 수정
```

### 3-6. 다이얼로그 오브 드림스 예약

```
GET    /api/cafe/:cafeId/dialogue/slots       -- 예약 가능 시간대 조회 (date 쿼리)
POST   /api/cafe/:cafeId/dialogue/book        -- 예약 (body: { date, startTime, roomNumber, guideName? })
GET    /api/cafe/dialogue/my-bookings         -- 내 예약 목록
DELETE /api/cafe/dialogue/:bookingId          -- 예약 취소
```

### 3-7. 카페 관리자용 (Admin)

```
GET    /api/admin/cafe/:cafeId/dashboard      -- 대시보드 (오늘 체크인 수, 벨 수, 이벤트 등)
POST   /api/admin/cafe/:cafeId/events         -- 이벤트 생성
PATCH  /api/admin/cafe/events/:eventId        -- 이벤트 수정
GET    /api/admin/cafe/:cafeId/members        -- 회원 목록
GET    /api/admin/cafe/:cafeId/doorbell-stats -- 도어벨 통계
```

---

## 4. 드림 플레이스 앱 내 UI 구현 — Cafe Tab

### 4-1. 네비게이션 구조

드림 플레이스 앱의 하단 탭 바에 **"Cafe" 탭**을 추가합니다.

```
[Home] [Match] [Projects] [☕ Cafe] [Profile]
```

Cafe 탭을 누르면 아래 화면들에 접근할 수 있습니다.

### 4-2. 카페 홈 화면 (Cafe Home)

**경로:** `/cafe`

```
┌─────────────────────────────────┐
│  ☕ Dream Cafe Gangnam           │
│  🟢 Open Now · 12 Dreamers here │
│                                 │
│  ┌─────────────────────────────┐│
│  │  📍 Check In                ││  ← 큰 체크인 버튼 (미체크인 시)
│  └─────────────────────────────┘│     체크인 후에는 "✓ Checked In" 표시
│                                 │
│  🔔 Today's Doorbell            │
│  ┌──────┐ ┌──────┐ ┌──────┐    │  ← 가로 스크롤 꿈 카드 미리보기
│  │ 꿈1  │ │ 꿈2  │ │ 꿈3  │    │
│  └──────┘ └──────┘ └──────┘    │
│  See all →                      │
│                                 │
│  📅 Today's Events              │
│  ┌─────────────────────────────┐│
│  │ 14:00 Planner Workshop      ││
│  │ 18:00 Dream Talk: 김OO 대표 ││
│  └─────────────────────────────┘│
│  See all →                      │
│                                 │
│  👥 Dreamers Here Now           │
│  [프로필1] [프로필2] [프로필3]  │  ← 현재 체크인한 드리머 아바타 목록
│  See all →                      │
└─────────────────────────────────┘
```

**조건부 렌더링:**
- 가까운 카페가 없으면: 전체 카페 목록(지도)을 보여주는 화면
- 카페가 1개만 있으면 (MVP): 바로 해당 카페 홈 표시
- 여러 카페가 있으면: 가장 가까운 카페를 기본 표시 + 상단에 카페 전환 드롭다운

### 4-3. 도어벨 화면 (Doorbell)

**경로:** `/cafe/doorbell`

**탭 구조:**

```
[모든 꿈 (All)] [지금 여기 (Here Now)] [내 꿈 (My Dream)]
```

**"모든 꿈" 탭:**

```
┌─────────────────────────────────┐
│  🔔 Doorbell                    │
│  [All] [Here Now] [My Dream]   │
│                                 │
│  Filter: [All Categories ▼]     │
│                                 │
│  ┌─────────────────────────────┐│
│  │ 📸 홍길동                    ││
│  │ "세계의 커피 농부를 돕는     ││
│  │  공정무역 브랜드를 만들고    ││
│  │  싶습니다"                   ││
│  │                              ││
│  │ 🏷 #food #social_enterprise  ││
│  │ 🔍 Need: #marketing #design ││
│  │                              ││
│  │ 🔔 23 rings  · 🟢 Here Now  ││
│  │                              ││
│  │ [🔔 Ring the Bell]           ││  ← 벨 누르기 버튼
│  └─────────────────────────────┘│
│                                 │
│  ┌─────────────────────────────┐│
│  │ 다음 꿈 카드...              ││
│  └─────────────────────────────┘│
└─────────────────────────────────┘
```

**"지금 여기" 탭:**
- 현재 카페에 체크인한 드리머의 꿈만 필터링
- 상단에 "🟢 Here Now" 뱃지 강조
- 이 탭이 도어벨의 핵심 — "지금 바로 만날 수 있는 사람"

**"내 꿈" 탭:**
- 내가 등록한 꿈 표시
- 등록/수정/삭제 가능
- 꿈 미등록 시: "Register Your Dream" 온보딩 화면

**벨 누르기 플로우:**
1. "Ring the Bell" 버튼 클릭
2. 선택적 메시지 입력 모달 ("왜 이 꿈에 관심이 있는지 한 줄 남겨주세요")
3. 전송 → 상대방에게 푸시 알림
4. 상대방이 수락하면 → 앱 내 채팅 또는 카페 내 만남 제안

### 4-4. 이벤트 화면 (Events)

**경로:** `/cafe/events`

```
┌─────────────────────────────────┐
│  📅 Events                      │
│  [Upcoming] [My Events] [Past]  │
│                                 │
│  This Week                      │
│  ┌─────────────────────────────┐│
│  │ 📌 Dream Talk               ││
│  │ 🗓 Tue, Feb 17 · 18:00      ││
│  │ 🎤 Speaker: 김OO 대표       ││
│  │ 👥 28/40 registered          ││
│  │ 💰 ₩15,000                  ││
│  │ [Register]                   ││
│  └─────────────────────────────┘│
│                                 │
│  ┌─────────────────────────────┐│
│  │ 🤝 Open Networking Night    ││
│  │ 🗓 Thu, Feb 19 · 19:00      ││
│  │ 👥 12/30 registered          ││
│  │ 💰 ₩10,000 (drink included) ││
│  │ [Register]                   ││
│  └─────────────────────────────┘│
└─────────────────────────────────┘
```

### 4-5. 다이얼로그 예약 화면 (Dialogue Booking)

**경로:** `/cafe/dialogue`

```
┌─────────────────────────────────┐
│  🌙 Dialogue of Dreams          │
│  "60 minutes of deep            │
│   self-discovery in darkness"   │
│                                 │
│  Select Date                    │
│  [◀ February 2026 ▶]           │
│  [13] [14] [15*] [16] [17]     │  ← 캘린더 날짜 선택
│                                 │
│  Available Slots (Feb 15)       │
│  ┌────────────────┐             │
│  │ 10:00 - 11:30  │ Room 1 ✅  │
│  │ 13:00 - 14:30  │ Room 2 ✅  │
│  │ 15:00 - 16:30  │ Room 1 ❌  │  ← 예약됨
│  │ 17:00 - 18:30  │ Room 2 ✅  │
│  └────────────────┘             │
│                                 │
│  Price: ₩50,000                 │
│  (Members: ₩45,000 / Pro: Free) │
│                                 │
│  [Book Now]                     │
└─────────────────────────────────┘
```

### 4-6. 만남 기록 화면 (Meeting Log)

**경로:** `/cafe/meetings`

```
┌─────────────────────────────────┐
│  👥 Meeting Log                  │
│                                 │
│  [+ New Meeting Record]        │
│                                 │
│  Today                          │
│  ┌─────────────────────────────┐│
│  │ 📸 이영희                    ││
│  │ Met via: 🔔 Doorbell         ││
│  │ "AI 교육 플랫폼에 대해       ││
│  │  이야기. 다음 주 미팅 약속"  ││
│  │ Follow-up: Schedule meeting  ││
│  │ [Edit] [Connect on Place]   ││  ← 드림 플레이스에서 연결하기
│  └─────────────────────────────┘│
│                                 │
│  Last Week                      │
│  ┌─────────────────────────────┐│
│  │ ...                          ││
│  └─────────────────────────────┘│
└─────────────────────────────────┘
```

---

## 5. 도어벨 디스플레이 (카페 대형 스크린용 웹앱)

### 5-1. 개요

카페 입구(Zone 1)에 설치된 55인치 터치스크린에서 돌아가는 **별도 웹앱**입니다.  
드림 플레이스 앱과 같은 백엔드를 공유하되, UI는 대형 스크린에 최적화됩니다.

**경로:** `/doorbell-display/:cafeId`  
**접근:** 카페 내 디스플레이 전용 (fullscreen 크롬 키오스크 모드)

### 5-2. 화면 구성

```
┌───────────────────────────────────────────────────────────────┐
│                                                               │
│  🔔 DREAM DOORBELL           Dream Cafe Gangnam               │
│  ─────────────────────────────────────────────────────────    │
│                                                               │
│  🟢 HERE NOW                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │  📸           │  │  📸           │  │  📸           │       │
│  │  홍길동       │  │  김영희       │  │  박철수       │       │
│  │              │  │              │  │              │       │
│  │ "세계의 커피  │  │ "모든 아이가  │  │ "AI로 교육   │       │
│  │  농부를 돕는  │  │  코딩을 배울  │  │  격차를 줄이  │       │
│  │  브랜드"     │  │  수 있는 세상" │  │  는 플랫폼"  │       │
│  │              │  │              │  │              │       │
│  │ #food        │  │ #education   │  │ #tech        │       │
│  │ 🔔 23        │  │ 🔔 15        │  │ 🔔 8         │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│                                                               │
│  ─────────────────────────────────────────────────────────    │
│  ALL DREAMS (auto-scrolling)                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │  ...          │  │  ...          │  │  ...          │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│                                                               │
│  ─────────────────────────────────────────────────────────    │
│  📱 Scan to register your dream     [QR CODE]                │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

### 5-3. 핵심 동작

- **자동 슬라이드:** 꿈 카드가 8초마다 자동 전환 (부드러운 애니메이션)
- **"Here Now" 우선:** 현재 카페에 체크인한 드리머의 꿈이 상단 영역에 고정
- **실시간 업데이트:** WebSocket으로 새 꿈 등록, 벨 울림을 즉시 반영
- **벨 울림 효과:** 누군가 벨을 누르면 → 해당 꿈 카드가 확대 + 벨 애니메이션 + 카운트 +1
- **터치 인터랙션:** 카드를 터치하면 해당 드리머의 상세 프로필 팝업
- **하단 QR 코드:** 드림 플레이스 앱 다운로드 / 도어벨 등록 페이지로 연결

### 5-4. 기술 구현

```
프레임워크: Next.js (드림 플레이스와 동일 프로젝트 내 별도 라우트)
실시간 통신: WebSocket (Socket.io 또는 Pusher)
애니메이션: Framer Motion
스크린 모드: fullscreen API + wake lock API (화면 꺼짐 방지)
```

### 5-5. WebSocket 이벤트

```typescript
// 서버 → 디스플레이 (수신)
'doorbell:new_dream'        // 새 꿈 등록됨
'doorbell:ring'             // 누군가 벨을 눌렀음 → 벨 애니메이션 트리거
'doorbell:dream_updated'    // 꿈 내용 수정됨
'doorbell:dream_removed'    // 꿈 삭제됨
'cafe:checkin'              // 새 드리머 체크인 → "Here Now" 영역 업데이트
'cafe:checkout'             // 드리머 체크아웃 → "Here Now" 영역에서 제거

// 디스플레이 → 서버 (송신)
'display:heartbeat'         // 디스플레이가 살아있음을 알림 (1분마다)
'display:touch_ring'        // 디스플레이 터치로 벨 누르기 (카페 방문자가 직접)
```

---

## 6. 카페 관리자 웹 대시보드

### 6-1. 개요

카페 직원/매니저가 PC에서 사용하는 관리 화면입니다.  
드림 플레이스의 관리자 패널에 "Cafe Management" 섹션으로 추가합니다.

**경로:** `/admin/cafe/:cafeId`

### 6-2. 대시보드 (Dashboard)

```
┌────────────────────────────────────────────────┐
│  Cafe Dashboard - Dream Cafe Gangnam           │
│                                                │
│  Today's Summary                               │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐         │
│  │  28  │ │  12  │ │   5  │ │   2  │         │
│  │Check │ │Bells │ │Event │ │Dialog│         │
│  │ ins  │ │Today │ │Regis.│ │Books │         │
│  └──────┘ └──────┘ └──────┘ └──────┘         │
│                                                │
│  Currently Checked In: 12 dreamers             │
│  [드리머 목록 테이블]                           │
│                                                │
│  Today's Events                                │
│  [이벤트 목록 + 참가자 수 + 체크인 현황]        │
│                                                │
│  Doorbell Activity (Last 7 days)               │
│  [벨 울림 트렌드 차트]                          │
└────────────────────────────────────────────────┘
```

### 6-3. 관리 기능

- **이벤트 CRUD:** 이벤트 생성, 수정, 취소, 참가자 관리
- **도어벨 모니터링:** 등록된 꿈 목록, 부적절한 꿈 신고 관리, 벨 통계
- **체크인 현황:** 실시간 카페 내 드리머 수, 일별/주별 통계
- **다이얼로그 예약 관리:** 오늘의 예약 목록, 노쇼 처리
- **회원 관리:** 멤버십 티어별 조회, 방문 빈도 통계

---

## 7. 드림 허브 생태계 연동

### 7-1. 드림 플레이스 ↔ 카페 (같은 앱이므로 내부 연동)

| 기능 | 연동 방식 | 설명 |
|------|-----------|------|
| 프로필 공유 | 동일 user 데이터 | 도어벨의 꿈, 프로필 사진 등은 플레이스 프로필에서 가져옴 |
| 매칭 데이터 반영 | doorbell_dreams → matching_engine | 도어벨에 등록한 꿈 카테고리/필요역량이 플레이스 매칭 점수에 반영 |
| 오프라인 만남 → 온라인 연결 | cafe_meetings → connections | 카페에서 만난 사람을 플레이스에서 정식 Connection으로 전환 |
| "Meet at Cafe" 제안 | matching 결과에 카페 추천 | 온라인 매칭된 드리머들에게 "가까운 드림 카페에서 만나보세요" 제안 |

### 7-2. 드림 브레인 연동 (API)

```
POST /api/brain/sync/cafe-meeting
Body: {
  userId: string,
  metUserId: string,
  memo: string,
  meetingContext: 'doorbell' | 'event' | 'spontaneous',
  cafeId: string
}
```
→ 만남 메모가 드림 브레인에 "관계" 카테고리로 자동 저장

### 7-3. 다이얼로그 오브 드림스 → 드림 브레인 연동 (API)

```
POST /api/brain/sync/dialogue-recording
Body: {
  userId: string,
  bookingId: string,
  recordingUrl: string,
  duration: number
}
```
→ 다이얼로그 체험 녹음이 드림 브레인으로 전송 → AI 분석 → "나와의 대화 리포트" 생성

### 7-4. 드림 플래너 연동

- 카페 이벤트 중 "Planner Workshop"에 참가한 드리머는 드림 플래너의 해당 활동에 "워크숍 참여 완료" 배지 자동 부여
- 드림 플래너 PART 4 "Dream 5 네트워킹" 활동이 카페의 Dream 5 매칭 이벤트와 연결

---

## 8. 실시간 기능 구현 (WebSocket)

카페 모듈은 실시간 업데이트가 핵심입니다. 아래 이벤트들을 WebSocket으로 처리합니다.

### 8-1. 채널 구조

```
cafe:{cafeId}              -- 특정 카페의 모든 실시간 이벤트
doorbell:{cafeId}          -- 특정 카페의 도어벨 이벤트
user:{userId}:cafe         -- 특정 사용자의 카페 관련 알림
```

### 8-2. 실시간 이벤트 목록

| 이벤트 | 트리거 | 수신자 | 데이터 |
|--------|--------|--------|--------|
| `cafe:checkin` | 드리머가 체크인 | 같은 카페 체크인한 모든 드리머 + 디스플레이 | { userId, userName, dreamStatement } |
| `cafe:checkout` | 드리머가 체크아웃 | 같은 카페 체크인한 모든 드리머 + 디스플레이 | { userId } |
| `doorbell:ring` | 벨 누르기 | 꿈 주인 + 디스플레이 | { ringerId, ringerName, dreamId, message } |
| `doorbell:ring_response` | 벨 응답 (수락/거절) | 벨 누른 사람 | { status, dreamOwnerId } |
| `doorbell:new_dream` | 새 꿈 등록 | 디스플레이 | { dreamId, dream data } |
| `event:update` | 이벤트 참가자 수 변경 | 해당 카페의 이벤트 화면 | { eventId, currentParticipants } |

---

## 9. 푸시 알림

| 상황 | 알림 제목 | 알림 내용 |
|------|-----------|-----------|
| 벨 받음 | "🔔 Someone rang your Doorbell!" | "[이름] is interested in your dream. Tap to see." |
| 벨 수락됨 | "🤝 Connection accepted!" | "[이름] accepted your bell. Start a conversation!" |
| 이벤트 리마인더 | "📅 Event starting soon" | "[이벤트명] starts in 1 hour at Dream Cafe" |
| 다이얼로그 리마인더 | "🌙 Dialogue session tomorrow" | "Your Dialogue of Dreams session is tomorrow at [시간]" |
| 근처 카페 알림 | "☕ Dream Cafe nearby" | "You're near Dream Cafe [지점]. 8 dreamers are here now!" |

---

## 10. 폴더 구조 (드림 플레이스 프로젝트 내)

```
src/
├── modules/
│   ├── cafe/                          # ★ 카페 모듈 전체
│   │   ├── components/                # UI 컴포넌트
│   │   │   ├── CafeHome.tsx           # 카페 홈 화면
│   │   │   ├── CafeStatusBanner.tsx   # 영업 상태 + 체크인 수 배너
│   │   │   ├── CheckInButton.tsx      # 체크인/아웃 버튼
│   │   │   ├── CurrentDreamers.tsx    # 현재 체크인 드리머 목록
│   │   │   └── CafeSelector.tsx       # 카페 지점 선택 드롭다운
│   │   │
│   │   ├── doorbell/                  # 도어벨 관련
│   │   │   ├── DoorbellList.tsx       # 꿈 목록 (필터 탭 포함)
│   │   │   ├── DreamCard.tsx          # 개별 꿈 카드 컴포넌트
│   │   │   ├── RingBellButton.tsx     # 벨 누르기 버튼 + 메시지 모달
│   │   │   ├── MyDreamEditor.tsx      # 내 꿈 등록/수정 폼
│   │   │   ├── RingsReceived.tsx      # 받은 벨 목록
│   │   │   └── RingsSent.tsx          # 보낸 벨 목록
│   │   │
│   │   ├── events/                    # 이벤트 관련
│   │   │   ├── EventList.tsx          # 이벤트 목록
│   │   │   ├── EventDetail.tsx        # 이벤트 상세 + 참가 신청
│   │   │   └── MyEvents.tsx           # 내 참가 이벤트
│   │   │
│   │   ├── dialogue/                  # 다이얼로그 예약
│   │   │   ├── DialogueBooking.tsx    # 예약 화면 (캘린더 + 시간 선택)
│   │   │   └── MyBookings.tsx         # 내 예약 목록
│   │   │
│   │   ├── meetings/                  # 만남 기록
│   │   │   ├── MeetingLog.tsx         # 만남 기록 목록
│   │   │   └── MeetingForm.tsx        # 만남 기록 작성/수정
│   │   │
│   │   ├── hooks/                     # 커스텀 훅
│   │   │   ├── useCafeStatus.ts       # 카페 상태 조회
│   │   │   ├── useCheckin.ts          # 체크인/아웃 로직
│   │   │   ├── useDoorbell.ts         # 도어벨 CRUD + WebSocket
│   │   │   ├── useCafeEvents.ts       # 이벤트 조회/참가
│   │   │   └── useCafeRealtime.ts     # WebSocket 연결 관리
│   │   │
│   │   ├── api/                       # API 호출 함수
│   │   │   ├── cafeApi.ts
│   │   │   ├── doorbellApi.ts
│   │   │   ├── eventApi.ts
│   │   │   ├── dialogueApi.ts
│   │   │   └── meetingApi.ts
│   │   │
│   │   ├── types/                     # TypeScript 타입
│   │   │   └── cafe.types.ts
│   │   │
│   │   └── utils/                     # 유틸리티
│   │       ├── cafeHelpers.ts
│   │       └── doorbellAnimations.ts
│   │
│   └── ... (기존 모듈들)
│
├── pages/ (또는 app/)
│   ├── cafe/                          # 카페 라우트
│   │   ├── index.tsx                  # /cafe → CafeHome
│   │   ├── doorbell/
│   │   │   └── index.tsx              # /cafe/doorbell
│   │   ├── events/
│   │   │   ├── index.tsx              # /cafe/events
│   │   │   └── [eventId].tsx          # /cafe/events/:id
│   │   ├── dialogue/
│   │   │   └── index.tsx              # /cafe/dialogue
│   │   └── meetings/
│   │       └── index.tsx              # /cafe/meetings
│   │
│   ├── doorbell-display/              # ★ 대형 스크린용 (별도 레이아웃)
│   │   └── [cafeId].tsx               # /doorbell-display/:cafeId
│   │
│   └── admin/
│       └── cafe/
│           └── [cafeId].tsx           # /admin/cafe/:cafeId
│
└── server/ (또는 backend/)
    ├── modules/
    │   └── cafe/
    │       ├── cafe.controller.ts
    │       ├── cafe.service.ts
    │       ├── cafe.module.ts
    │       ├── doorbell.controller.ts
    │       ├── doorbell.service.ts
    │       ├── event.controller.ts
    │       ├── event.service.ts
    │       ├── dialogue.controller.ts
    │       ├── dialogue.service.ts
    │       ├── meeting.controller.ts
    │       ├── meeting.service.ts
    │       └── cafe.gateway.ts        # WebSocket gateway
    │
    └── database/
        └── migrations/
            └── YYYYMMDD_create_cafe_tables.ts
```

---

## 11. 구현 우선순위

### Phase 1: 핵심 기능 (먼저 만들 것)
1. ✅ DB 마이그레이션 (cafes, cafe_checkins, doorbell_dreams, doorbell_rings 테이블)
2. ✅ 카페 홈 화면 + 체크인/체크아웃
3. ✅ 도어벨 - 꿈 등록/조회/벨 누르기 (앱 내)
4. ✅ 도어벨 디스플레이 웹앱 (기본 슬라이드 + 실시간 업데이트)
5. ✅ 푸시 알림 (벨 울림)

### Phase 2: 확장 기능
6. 이벤트 CRUD + 참가 신청
7. 만남 기록
8. 다이얼로그 예약
9. 관리자 대시보드 (기본)

### Phase 3: 연동 & 고도화
10. 드림 브레인 연동 (만남 메모 동기화)
11. 드림 플래너 연동 (워크숍 배지)
12. 글로벌 도어벨 (여러 카페 간 꿈 공유)
13. 관리자 대시보드 (통계, 차트)

---

## 12. 주의사항

- **별도 앱 만들지 마세요.** 모든 카페 기능은 드림 플레이스 앱 내 모듈입니다.
- **도어벨 디스플레이만 별도 웹앱**으로 만듭니다. (같은 프로젝트, 다른 라우트)
- 드림 플레이스의 기존 인증 시스템(Dream ID)을 그대로 사용합니다.
- 기존 users 테이블에 카페 관련 컬럼을 추가하지 않습니다 — 모든 카페 데이터는 별도 테이블에 저장합니다.
- WebSocket 연결은 카페 탭에 진입할 때만 연결하고, 나갈 때 해제합니다 (리소스 절약).
- 도어벨 디스플레이 웹앱은 항상 연결 상태를 유지해야 하므로 자동 재연결 로직이 필수입니다.
- All UI text must be in **English** for global accessibility.
